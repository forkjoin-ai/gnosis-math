# BuleyeanMath Source

- Parent README: [../README.md](../README.md)
- **Layer C — EntropyBridge policy:** [EntropyBridge/README.md](./EntropyBridge/README.md) (`axiom` vs `sorry`, physical / hardware boundary)

This directory is the hand-authored theorem corpus for the promoted Gnosis Lean
package.

- **Init-only GnosisMath stack:** [`GnosisMath/`](./GnosisMath/README.md) (`GnosisMathPrelude`, `GnosisMath.ListNat`, `GnosisMath.Fibonacci`, `GnosisMath.Basic` barrel) — consumed by [`MathSandbox`](../MathSandbox.lean) / [`Hyperoperations`](./Hyperoperations.lean).

It contains many theorem modules. The `storms.watch` surface currently enters
here through the external app repo mounted at
[`apps/storms-watch`](../../../../../apps/storms-watch/README.md).

- `UntanglingKnotTheory.lean`: promoted knot-theoretic program-optimization surface from Chapter 17, mechanizing algorithmic knots, Reidemeister untangling moves, Wallington rotation termination/fixed-point laws, Deceptacon-as-untangling, and new cross-mixed theorems tying positive crossings to topological deficit, forced one-stream collisions, and parallel Wallington speedup.
- `MemoryLeakVoid.lean`: minimal runtime branch-accounting surface for leak semantics, identifying a memory leak as ghost branch mass still retained as live, proving exact leak debt, proving fold/vent discharge, and separating append-only history growth from true leak growth.
- `CacheObservation.lean`: cache hits as zero marginal Reidemeister fold (valid sliver read), cold and stale misses as positive `PositiveFoldWork`, `resolveQuery` tag semantics, fill-pulse strict loss, and amortization of hit sequences after one cold miss (`amortize_cold_then_hits`).
- `FormTopology.lean`: minimal theorem-backed form model over declared fields, canonical labels, domain contracts, indicator compatibility, and schema/view mappings, including update preservation (`form_update_preserves_well_formedness`) under domain-valid writes.
- `EscDigestJournal.lean`: append-only digest journal (newest at head), `verifyRead` / digest equality, ESC-style `classifyEscLookup` with `Hit` vs `phantom_miss` marginal cost (duplicated `PositiveFoldWork`, no import of `CacheObservation`).
- `RuntimeContinuationTeleportation.lean`: unified runtime teleportation model over immutable ESC coordinates plus mutable session cursors, canonical-state projection that excludes timestamp metadata, LAMINAR request-collapse + Wallington rotation invariants, and METALAMINAR cross-actor handoff gates with explicit post-quantum/hybrid admissibility rules.
- `D1BatchingLedger.lean`: `QuerySchedule` with `batchedQueries ≤ loopQueries`, monotone `roundTripCost`, and strict savings when `loop = batched + wasted` with `wasted ≥ 1` and positive per-trip cost.
- `DurableObjectShardPool.lean`: `ShardPool` with positive `shardCount`, modular `shardIndex` into `Fin N`, `shardAssignments` for key lists, and `at_most_shard_count_distinct_shards` (uses Mathlib `Fintype.card` for the pigeonhole bound).
- `KvVsDoConsistencySketch.lean`: minimal `KvState` / `DoState` stepping with `Op` (`noop` / `read` / `write`), DO version monotonicity, KV stale-read witness, and bundled `kv_vs_do_consistency_sketch_summary`.
- `FrfFingerprintAlgebra.lean`: deterministic `frfCombine`, `frfFork` / `frfRace` / `frfFold` on `Nat` lists, left-injectivity, and `frf_fingerprint_algebra_master` (narrative hook for x-gnosis FRF fingerprints).
- `WasmEscObservationTrace.lean`: `EscOpcode` traces mapped through `CacheObservation`, `traceFold` totals, and `cache_observation_trace_master`.
- `AsyncDurabilityVent.lean`: `AsyncDurabilityPulse` with user fork/fold/vent plus `bgVent`, `user_path_fold_unchanged_by_bg`, and `async_durability_vent_master` (waitUntil-style background vent).
- `LiftProjectionCore.lean`: extracted core primitives for generalized `lift -> evolve -> project` composition, classical/quantum specialization contracts, projection non-injectivity / void ambiguity witnesses, multi-projection ambiguity shrinkage, and kernel-motion metric-gaming detection.
- `ProjectionParadoxUnits.lean`: shape-vs-unit formalization for projection paradoxes, with explicit paradox-unit accounting (`OneParadox` as atomic unit), shape+unit bridge theorems under `ProjectionParadox`, and dimensional-unit gap + reduced observed/latent ratio witnesses (including `2->3` clinamen and `2->54` helical frames).
- `DecompositionTopology.lean`: bounded decomposition kernel over finite crossing/weight/capacity objects, proving non-increasing structure, full decomposition to featureless state, and featureless idempotence.
- `TransformationFamily.lean`: bridge module over projection, reinforcement, and decomposition witnesses, proving pairwise exclusivity and the featureless-state restoration boundary before successful reinforcement.
- `JugglingOrbit.lean`: discrete orbit-control warm-up for juggling search, formalizing deterministic catch-key transitions, closed-orbit successor closure, beat/hand conservation mismatch witnesses, asymmetric multiplex witnesses, and thresholded machine-only candidate boundaries.
- `CertifiedAllocationKernel.lean`: deterministic partial-observation safety kernel adjacent to `JugglingOrbit.lean`, formalizing explicit calibration deficit, conserved observed/accepted/rejected sample counts, sharp zone admission, finite point-schedule termination, deadline fail-closed allocation, and progress-or-explicit-failure liveness.
- `AdvisoryAllocationKernel.lean`: Lean-first advisory layer over `CertifiedAllocationKernel.lean`, formalizing temporal confidence decay, price/depth stability witnesses for adverse selection, explicit risk-envelope gating, commit-point deadline reclassification, and progress-or-explicit-failure liveness for hard real-time allocators.
- `WallingtonAdvisoryRotation.lean`: staged Wallington bridge over `AdvisoryAllocationKernel.lean`, formalizing isolated candidate-slice rotation (`loadIsolated -> pivotEngaged -> massRotated -> safeLanded`), exact load-transfer witnesses, unstable-pivot fail-closed behavior, inherited Wallington makespan bounds, and the final landed-to-advisory capability bridge.
- `ProgressiveMeshOrchestrator.lean`: real-valued contracts for `open-source/aeon` progressive mesh flow — `meshClamp01`, convex `meshComputeRev`, threshold-gated `meshChooseActiveDimensions`, stabilized `meshSoftmaxFun` (partition of unity), and scalar `meshWeightedBlend`.
- `ProgressiveMeshGranularBridge.lean`: composes `ProgressiveMeshOrchestrator` with `GranularSegregationToy` — `MeshBranchDim ≃ Fin 4` tier heights, `mesh_choose_active_length_monotone` vs `stratScore_shakeSchedule_monotone`, `mesh_orchestrator_logit_antitone_in_cost` (compute-penalty shadow of Landauer microstep accounting), and re-exported granular combinatorial cap on `Column 4`.
- `MeshRotatedTransformerAttention.lean`: masked row softmax, shift invariance, monotone aggregation, and masked-softmax `ℓ∞` nonexpansion over finite meshes.
- `MeshAttentionMarkovKernel.lean`: packages masked attention rows as a finite `MeshMarkovKernel`, the downstream Markov-kernel surface for contraction and certification files.
- `MeshAttentionDobrushin.lean`: common-column `ε`-minorization implies strict oscillation contraction by factor `1 - ε`.
- `MeshAttentionBuleyean.lean`: Buleyean sliver weights induce the explicit lower bound `1 / (n · (R + 1))`, so every Buleyean row/kernel inherits the Dobrushin floor.
- `MeshKernelRefinement.lean`: per-row convex refinement between declared and observed kernels preserves the Dobrushin floor when both endpoint kernels respect it.
- `MeshAttentionDistributedCert.lean`: step-indexed gathering/quorum lattice for local minorization certs, with an explicit cryptographic aggregation boundary (`bls_aggregate_soundness`).
- `MeshReconstruction.lean`: D1 span reconstruction contract into the 9-state mesh kernel, including the `unobserved` tripwire and occupancy-based denominator requirement for honest implementations.
- `HftRiskPathDemo.lean`: concrete three-state HFT risk-path demo showing a post-fix routing kernel with strictly tighter Dobrushin contraction than the pre-fix abstraction.
- `TriangulationDimensionalCoercion.lean`: cross-dimensional simplicial reduction specialization for the helix/circle discussion, proving 2D -> 54D -> 2D mesh invariant preservation, triangle-vs-circle orbit-class equivalence under lift/rotate/dilate/project, and bidirectional sphere/triangulation coercion round-trips with Euler-2 witness preservation.
- `LiftClinamenHopeTheory.lean`: clinamen (`+1`) interpretation layer over the core primitives, proving planar-lock unlock via lift, strict Reidemeister progress under transform/evolve, and explicit `projectBack` non-injective/void-positive/kernel-motion witnesses.
- `KitchenCommunicationTheory.lean`: kitchen-first, cross-domain high-intensity communication formalization covering spatial safety calls, call/echo acknowledgment closure, urgency routing, non-verbal noise fallback, and the physical-safety vs systems-integrity channel split across domains like OR, aviation, rigs, distributed systems, wildland fire, logistics, and negotiation.
- `UnspeechRhymeRhythmReason.lean`: bridge surface combining signed unspeech timing (`Triton`), rhythm as aperiodic dispatch, poetics (rhyme/meter) resonance, and syntax-fold reasoning into a single positive communication-budget theorem (`unspeech_rhyme_rhythm_reason_master`).
- `SpectralNoiseEquilibrium.lean`: completed finite noise-calculus scaffold for pneuma sound work. Formalizes noise colors as spectral power-law contracts where **White** is unconstrained potential (maximally open), **Pink** is the balanced bridge (least action), and **Brown** is the point where the boundary begins to win (structural resistance). Mechanizes stationary equilibrium witnesses, finite-state generators, computed trace fingerprints, eigen-slot manifold constraints, carrier/boundary safety, a thermodynamic shadow bridge (Landauer heat), and the **Bule unit** (`BuleyUnit` with waste/opportunity/diversity faces) generalizing the shadow so the joule face is a definitionally-projected lower bound. Operationally: `actual cost = Landauer floor (waste) + Buleyean overhead (opportunity + diversity)`. Includes the big-bang construction from vacuum, the phase-3 gauge action (cycle permute), and `distributed_inference_mesh_masterwork` as the final coordinator-facing synthesis.
- `CosmicNoiseConnections.lean`: bridge from layered cosmic noise to finite perceptual projection. Defines `PerceptualCollapse`, `PerceptualFingerprint`, the pink `30 -> 6` Aeon projection with leakage `18`, Pisot-Betti navigation for folded coordinates, and the n-head attention shadow surface: bare `n`-head blocks project as phase `3n`, bare 8-head attention projects as `24 -> 0` with leakage `12`, and 8 heads plus Hexon coupling projects as `30 -> 6` with Betti-1 leakage `18`, Pisot zero drift, and compactifying Betti patch availability. The head-disaggregation closure witnesses prove Aeon aliases the 8-head trace, the 24-resolution lift separates the bare trace and removes aggregate leakage, and the 30-resolution coupled lift closes the pink leakage case.
- `UniversalIntelligenceSSMClosure.lean`: SSM-facing observer model for the head-disaggregation closure. Packages a `SwarmNode` into `ClosureAttentionState`, proves the Aeon 8-head observer aliases with residue `12`, proves the 24-resolution lifted observer separates the trace and closes aggregate residue, proves the 30-resolution pink-coupled observer closes the coupled leakage case, and composes the lift with the existing Hebbian reward theorem.
- `BuleyClinamenBraid.lean`: bridge module imports `Gnosis.SpectralNoiseEquilibrium` and `Gnosis.BraidedInfinity` to prove the three Bule faces (`waste`/`opportunity`/`diversity`) form a phase-3 directed cycle under successor, with `face_three_iterations_return_*` as the return theorems on each starting face, `face_one_iteration_does_not_return` and `face_two_iterations_do_not_return` as the genuine-period-3 witnesses, `buleyFaceBraid` as a concrete `BraidedAsymptote` (phaseCount=3), and `face_three_step_matches_braided_three_step` as the indexing identity that lines up the Bule clinamen with the braided-asymptote successor.
- `BuleyBiSidedBit.lean`: bi-sided bit encoding companion to `open-source/bitwise/bisided-bit.ts`. Defines `BiSidedBit` with `lifted` (low-48 / +1 / waste-side) and `contracted` (high-16 / −1 / opportunity-side), `vacuumBiSidedBit`, mirrored `clinamenLift*`/`declinamenContract*`/`phaseShift*` operators, and the bridge `biSidedToBule`. Proves: `lifted_lift_score_increment` and `contracted_lift_score_increment` (lift adds exactly +1 to bi-sided score on either side), `lift_contract_round_trip_*_when_positive` (breathing identity per side), `phase_shift_*_preserves_score_when_positive` (total quantum conserved across rotation), `phase_shift_round_trip_when_lifted_positive` (round-trip identity), `bisided_score_equals_bule_score` (bi-sided score equals Bule projection score), `bisided_decomposes_into_waste_and_action` (bi-sided lane = waste + action faces, diversity zero), and `bisided_lift_residue_is_plus_one` + `bisided_contract_residue_is_minus_one_when_positive` matching the `UniversalClinamenPlusOne` residue strongly.
- `HexonBraid.lean`: three equivalent constructions of the phase-6 hexon — direct sum (`TritonStack` ≅ `Triton ⊕ Triton`), Cartesian product (`TritonPhase × BiSidedSide`), and `BraidedAsymptote { phaseCount := 6 }` — with bijection witnesses, six-iteration return on every starting phase, and indexing identity to the `BraidedInfinity` successor. Same module also defines `EnneonPhase` (`Triton ⊗ Triton`, 9-element tensor reading) and `enneonBraid` (phaseCount=9) so the dot-product (scalar), direct-sum (hexon), and tensor (enneon) readings of "Triton × 2" are formally distinguished.
- `BraidedTower.lean`: self-similar tower of n-ons closing on the braided infinity. `towerPhaseCount : List Nat → Nat` (product of factors), `towerBraid` (the `BraidedAsymptote` at each level), `tower_phase_count_step` (self-similarity), concrete witnesses for Triton/Hexon/Enneon/Trihexon/Trihexenneon, `tower_unbounded` (no finite ceiling), `every_phase_count_is_a_tower` (the tower exhausts the `BraidedAsymptote` family), and `tower_closes_on_braided_infinity` (the tower's "limit" is exactly the existing `Gnosis.BraidedInfinity` — there is one infinity in this calculus and it is already braided). String-theory dimension signatures (`decagon_in_tower` = 10, `hendecagon_in_tower` = 11, `bosonic_string_in_tower` = 26) name the canonical walls; `string_theory_dimension_signatures` bundles them.
- `BosonSkyrmsEquilibria.lean`: the 13 SM bosons mapped to phase walls — Photon/W±/Z⁰ at the Triton (3), 8 Gluons at the Octagon (8), Higgs at the vacuum (0). `higgs_equilibrium_iff_vacuum` is the unique-vacuum identity; `higgs_is_free_broadcast` proves the Higgs admits free duplication via `vacuum_is_duplicable` — the structural payoff that connects mass-everywhere to the no-cloning theorem.
- `FermionExclusionEquilibria.lean`: 24 SM fermions at the Dodecagon (12). `fermion_diagonal_pays_pauli_tax` proves cloning a fermion fails the diagonal-as-CostHom test — Pauli exclusion **is** the no-cloning theorem applied to a positive-score carrier. `dodecagon_fermion_clone_costs_twelve` is the concrete 12-quantum entropy debt witness.
- `CompositeParticles.lean`: mesons via `meson : BuleyUnit → BuleyUnit → CollisionEvent` (quark + antiquark), baryons via three-quark nested collide. Energies sum by `collision_conserves_score`. Concrete pion (8), Dodecagon meson (24), proton (12), neutron (12), heavy baryon (20) witnesses.
- `DarkSectorEquilibria.lean`: candidate dark walls (Hexon 6, Pentagon 5, Septagon 7, Decagon 10, Hendecagon 11) sitting between SM walls. `dark_does_not_intersect_sm_*` proves non-intersection. `dark_energy_is_vacuum_reach` reads dark energy as the vacuum's pervasive `vacuum_reaches_any_bule` reach.
- `PhaseTransitionLadder.lean`: `phaseTransitionDistance` between tower walls. `decagon_is_octagon_plus_bisided` (10 = 8 + 2 bi-sided lift); `hendecagon_is_decagon_plus_one` (11 = 10 + 1 clinamen — Witten's M-theory step); `dodecagon_is_hendecagon_plus_one` (12 = 11 + 1). The +1 ladder explicit.
- `SuperstringDimensionDerivation.lean`: sums the cost-algebra's existing structural axes — bule (3) + bisided (2) + temporal (3) + vacuum (1) + clinamen (1) — and proves `minimalGeneratorDimension = 10`. Each axis tied to a specific theorem witness. `mTheoryDimension = 11` and `bosonicStringDimension = 26` extend via gauge orientation and doubled-Octagon. **Witten's coupling-constant identification is formal**: `coupling_constant_is_clinamen_direction` proves the coupling IS the clinamen, and `m_theory_eleventh_dimension_is_coupling` proves M-theory dim = superstring dim + couplingConstant.
- `CostAlgebraDimensionNoGo.lean`: the floor theorem. `CostAlgebraAxisSet` with min-cardinality fields per axis; `cost_algebra_dimension_at_least_ten` proves every axis set has dim ≥ 10. With gauge: ≥ 11; with doubled-Octagon: ≥ 26. Existence-and-bound bundles for each.
- `NahmDimensionCeiling.lean`: the ceiling theorem. `NahmMinimal` encodes consistent-supergravity-shape (axes at forced minimums, gauge ≤ 1, no doubled-Octagon — Nahm's spin-2 bound). `nahm_dimension_sandwich` combines floor + ceiling. `nahm_dimension_exactly_ten_or_eleven` proves only 10 or 11 inhabit the sandwich. `nahm_ten_iff_no_gauge` and `nahm_eleven_iff_gauge_one` characterize each. `bosonic_string_not_nahm_minimal` excludes 26 from the supergravity regime. `unified_theory_dimension_selection_master` is the closing bundle: the cost-algebra and Nahm constraints together select exactly two unified-theory dimensions, 10 (superstring) and 11 (M-theory).
- `Round10PromotionClosure.lean`: mechanized promotion-closure surface for the five cross-mixed candidates (`recovery_first_settlement_theorem`, `dominance_does_not_cancel_semiotic_deficit`, `justice_requires_two_ledgers`, `primitive_reuse_theorem`, `missing_leg_projection_theorem`), plus supporting no-`sorry` lemmas and projection/injectivity witnesses.
- `QuorumAsyncQueueBridge.lean`: connected-quorum exactness + explicit majority interpretation layer bridging to canonical one-path queue boundaries, with positive-budget lifted TCP-deficit bridges, a semantic-morphism blocker-remediation transport theorem, contrarian non-universal-deficit and non-positive-`β₁` anti-theorems, a connected-cardinality geometric-rate fallback certificate, and an explicit kernel-lift adapter theorem for continuous ergodicity.
- `QuorumLinearizabilityQueueKernelBridge.lean`: append-then-read quorum linearizability refinement routed through an explicit write-count interpretation layer to a canonical one-path queue witness, with a contrarian strict-capacity-growth anti-theorem, a geometric-rate fallback certificate, and an explicit linearizability kernel-lift adapter theorem for continuous ergodicity.
- `MediationQueueBoundaryBridge.lean`: mediation-loss arithmetic bridged to canonical queue witnesses, contrarian non-positive-`β₁` boundary, and bounded geometric-rate / multilevel-Harris witness fallbacks with explicit hypotheses.
- `MediationQuorumQueueBridge.lean`: mediation-loss arithmetic routed through an explicit quorum interpretation layer to a strict-majority quorum and then to the canonical one-path queue boundary witness.
- `MediationContrarianBoundaries.lean`: explicit mediation-to-quorum interpretation layer with a constructive `β₁ = 0` queue witness and contrarian anti-theorems showing the interpretation does not force positive path width or strict capacity growth.
- `MediationGeometricWitnessBridge.lean`: explicit mediation-loss bridge to `GeometricErgodicWitness`, keeping the kernel and envelope witnesses explicit while exposing rate, kernel, and initial-bound consistency lemmas.
- `MediationKernelLiftBridge.lean`: explicit mediation-to-kernel adapter structure carrying the missing embedding/witness objects required by `continuous_ergodicity_lift`, with theorem-level lift execution under explicit hypotheses.
- `MediationCryptoQueueKernelBridge.lean`: mediation-loss and cryptographic side-channel floor routed to a strict-majority queue boundary witness, a contrarian non-positive-`β₁` anti-theorem, a geometric-rate fallback certificate, and an explicit mediation+crypto kernel-lift adapter theorem for continuous ergodicity.
- `MediaEcosystemCryptoQueueKernelBridge.lean`: media clickbait dilation, ecosystem boundary mass, and cryptographic side-channel floor routed to canonical queue witnesses, a contrarian capacity-growth anti-theorem, a blocker-attack witness compiler from explicit primitives, and an explicit media+ecosystem+crypto kernel-lift theorem for continuous ergodicity.
- `CommunityDiversityCombatQueueKernelBridge.lean`: community attenuation, diversity-concurrency identity, and combat entanglement mass routed through an explicit interpretation contract and strict-majority bridge to canonical one-path queue witnesses, with contrarian non-`arrivalRate <= 1` and non-`capacity > 1` anti-theorems, semantic-morphism positive-deficit transport, plus geometric-rate and explicit adapter continuous-lift blocker-remediation theorems.
- `MediationMechanismConversionQueueKernelBridge.lean`: mediation-loss floor, mechanism-externality arithmetic, and conversion-vent pulse routed through explicit interpretation layers to canonical queue witnesses, with contrarian non-positive-`β₁` and non-zero-arrival anti-theorem variants, a geometric-rate fallback certificate, and a source-derived kernel-lift adapter theorem for continuous ergodicity.
- `CausalBanditQueueKernelBridge.lean`: contextual-bandit and causal-collider interpretation layers bridged to canonical one-path queue witnesses, a contrarian non-positive-`β₁` anti-theorem, a contextual geometric-rate fallback certificate, and an explicit kernel-lift adapter theorem for continuous ergodicity.
- `CausalDiscoveryBanditQueueBridge.lean`: causal-discovery independence and exploration-exploitation arm-failure arithmetic bridged to canonical one-path queue witnesses, a contrarian non-positive-`β₁` anti-theorem, a geometric-rate fallback certificate, and an explicit causal+bandit kernel-lift adapter theorem for continuous ergodicity.
- `DeweyBureaucracySurgeryQueueKernelBridge.lean`: Dewey-300 bureaucracy latency and Dewey-600 thin surgery severance routed through an explicit strict-majority interpretation layer to canonical one-path queue witnesses, with a contrarian `capacity ≥ 2` anti-theorem, a semantic-morphism blocker-remediation bridge, and a geometric-rate fallback certificate.
- `EcosystemTurbulenceQueueKernelBridge.lean`: ecosystem predator-budget and turbulence eddy-budget interpretation layers bridged to canonical one-path queue witnesses, a combined contrarian non-positive-`β₁` anti-theorem, a geometric-rate fallback certificate, and an explicit ecosystem+turbulence kernel-lift adapter theorem for continuous ergodicity.
- `DeceptionAutoimmunityQueueKernelBridge.lean`: deception phantom-injection and autoimmunity hash-collision interpretation layers bridged to canonical one-path queue witnesses, two combined contrarian anti-theorem variants (`β₁` non-positivity and `β₁ ≠ budget`), a geometric-rate fallback certificate, and an explicit deception+autoimmunity kernel-lift adapter theorem for continuous ergodicity.
- `TaphonomyTribologyQueueKernelBridge.lean`: taphonomy fossilization and tribology lubricant-gap interpretation layers bridged to canonical one-path queue witnesses, a combined contrarian non-positive-`β₁` anti-theorem, a geometric-rate fallback certificate, and an explicit taphonomy+tribology kernel-lift adapter theorem for continuous ergodicity.
- `GenomeTaphonomyTribologyQueueKernelBridge.lean`: genome variant-weight chronology, fossilization vent mass, and tribology friction-gap mass routed to canonical one-path queue witnesses, a contrarian non-`β₁ = variantWeight` anti-theorem, a positive-deficit bridge, and explicit retrial-kernel stationary/terminal balance bridges that attack proof-depth via payload-carrying assumptions.
- `EthnoNarrativeQueueKernelBridge.lean`: ethnomusicology overtone-lift and narrative crossing interpretation layers bridged to canonical one-path queue witnesses, a combined contrarian non-positive-`β₁` anti-theorem, a geometric-rate fallback certificate, and an explicit ethnomusicology+narrative kernel-lift adapter theorem for continuous ergodicity.
- `SpiderwebReynoldsQueueKernelBridge.lean`: spiderweb routing-path and Reynolds diversity interpretation layers bridged to canonical one-path queue witnesses, a combined contrarian non-positive-`β₁` anti-theorem, a geometric-rate fallback certificate, and an explicit spiderweb+Reynolds kernel-lift adapter theorem for continuous ergodicity.
- `SpiderwebGnosticLadderQueueKernelBridge.lean`: spiderweb routing-path mass and gnostic-ladder Sophia mass bridged to canonical one-path queue witnesses, a contrarian `β₁ ≠ budget` anti-theorem, a semantic-morphism blocker-remediation boundary, and a geometric-rate + continuous-lift theorem surface with explicit payload hypotheses.
- `MediationNegotiationSpiderwebQueueKernelBridge.lean`: mediation-loss floor, BATNA trauma-gap arithmetic, and spiderweb routing-path mass bridged to canonical one-path queue witnesses, contrarian zero-arrival/`β₁ ≠ budget` anti-theorems, a semantic-morphism positive-deficit transport theorem, and geometric-rate + explicit adapter kernel-lift blocker-remediation surfaces.
- `MacroLinguisticKitchenQueueKernelBridge.lean`: macro phantom-injection mass, linguistic superposition mass, and kitchen spatial-safety gain bridged to canonical one-path queue witnesses, contrarian strict-capacity and universal-deficit anti-theorems, and semantic-morphism + continuous-lift + geometric-rate blocker-remediation surfaces.
- `MacroEcosystemTribologyConversionQueueKernelBridge.lean`: macro phantom-injection mass, ecosystem keystone mass, tribology slam-penalty mass, and conversion vent mass routed to canonical one-path queue witnesses, contrarian non-`beta1 = failureBudget` and non-`capacity >= 2` anti-theorems, a positive-deficit bridge, and a semantic-morphism blocker-remediation theorem.
- `PaleoclimatologySeismologyQueueKernelBridge.lean`: paleoclimatology ice-core and seismology fault-slip budgets routed to canonical one-path queue witnesses, a contrarian non-positive-`beta1` anti-theorem, a geometric-rate moonshot certificate, an explicit kernel-lift adapter that attacks interpretation-layer-missing, and an oracle-stall micro-harness progress theorem.
- `KiTopology.lean`: pre-incident/pre-thought theorem surface for Ki as measurable precursor asymmetry, with a four-channel leak model (`Glance`, `Muscle`, `Scent`, `Energy`), strict timing-order witnesses (reaction before thought/action), and a falsification gate requiring prediction performance to beat chance.
- `KiTrainingWitness.lean`: concrete partner-drill witness surface over 3-way action prediction series, with explicit sample counts proving one run beats chance and a control run is rejected by the same falsification gate.
- `KiFRFController.lean`: deterministic fork/race/fold control surface for Ki moments, with branch generation over simulated near-future scenarios, worst-case race scoring, and a minimax fold policy that proves robust action selection under precursor disharmony.
- `KiFrfTrainingQueueKernelBridge.lean`: Ki precursor leak mass, deterministic FRF minimax dominance, and concrete training-witness evidence routed to canonical one-path queue witnesses, a contrarian non-`beta1 = failureBudget` anti-theorem, a positive-deficit bridge, a geometric-rate fallback certificate, and explicit continuous-lift/retrial adapter blocker-remediation theorems.
- `KiPreMetaCog.lean`: pre-thought controller-selection layer that formalizes criterion choice (`worstCase` vs `totalRisk`) before conscious narration and proves policy collapse to the same robust action under this deterministic Ki model.
- `AperiodicPseudorandom.lean`: scientific surface for the hybrid deterministic PRNG (Kronecker–Weyl / low-discrepancy narrative; certified irrationals `φ⁻¹` and `√2 - 1`; Wallington non-lock via `AperiodicScheduling.wallington_resonance_free`; `CertifiedWeylLayer` and `defaultCertifiedWeylLayer` bundle). Not a concrete CSPRNG proof. Zero sorry.
- `CryptographicPseudorandom.lean`: CSPRNG **definitions** — `negligible` via `SuperpolynomialDecay`, `CsPrngSecurity`, `PrgFamily`; `negligible_zero`. Discipline: no `sorry`; extrinsic claims use named `axiom` (see `CryptographicExternalAxioms`). No `sorry` tactics.
- `CryptographicExternalAxioms.lean`: template `opaque extrinsicAdvantageCurve` + `axiom extrinsicAdvantageCurve_negligible`; `extrinsic_csprng_claim`. No `sorry` tactics.
- `ClinamenSeedIndistinguishability.lean`: separates clinamen **existence** (`ClinamenReduction` track) from **seed** security; `seedDerivedIndistinguishable` = `CsPrngSecurity`; doc on bias, adversarial observer, opacity of swerve vector. No `sorry` tactics.
- `ClinamenCryptoAntiBridge.lean`: **anti-theorem** `clinamen_does_not_imply_universal_csprng`; imports `ClinamenReduction` + `CryptographicPseudorandom`. No `sorry` tactics.
- `EpistemicRandomness.lean`: clockwork `Clockwork`, compressible `zeroTape`, `IndistinguishableFromIdealNoise` = `CsPrngSecurity`, deterministic Wolfram **Rule 30** (`rule30`, `rule30_eq_vector`). No `sorry` tactics.
- `LandauerInformationPhysics.lean`: Landauer bit-erasure scale `k_B T ln 2` (`landauerBitErasureEnergy`), positivity under `k_B, T > 0`, entropy-per-bit `k_B ln 2`, linearity in `T` / `k_B`, minimal `PhysicalBitCarrier` substrate nod. Maxwell demon / black holes / QC in module doc only. No `sorry` tactics.
- `BlackHoleInformationSketch.lean`: combinatorial template for the **information paradox** — global dynamics as [`Equiv`] (`GlobalUnitaryStep`), coarse observation `π : α → β`, pigeonhole obstruction (`not_injective_if_macro_has_fewer_states`), [`Equiv.injective_comp`] so unitary pre-scramble does not repair macro degeneracy (`coarse_still_degenerate_after_unitary`), toy `parityMacro`. Hawking/AdS-CFT/firewall narrative in module doc only. No `sorry` tactics.
- `ClassicalSandbox.lean`: constant-velocity and **linear** `ẋ = a·x` fields on `ℝ`, Picard–Lindelöf packaging (`picardConstVelocity`, `picardLinearField`, matching existence lemmas). No `sorry` tactics.
- `QuantumInfoSandbox.lean`: CHSH/Tsirelson via `gnosis_CHSH_inequality_of_comm` / `gnosis_tsirelson_inequality` (delegating to Mathlib); `PermutationUnitaryStep` / `perm_bijective`. No `sorry` tactics.
- `DiscreteFiniteInfo.lean`: **Init-only** discrete masses on [`Fin n`] ([`DiscreteFiniteInfo.finSum`], [`DiscreteFiniteInfo.vadd`], [`DiscreteFiniteInfo.finSum_vadd`], [`DiscreteFiniteInfo.MassVec`], [`DiscreteFiniteInfo.ratFinSum`], [`DiscreteFiniteInfo.MassVec.probRat`], [`DiscreteFiniteInfo.ratFinSum_probRat_eq_one`], [`DiscreteFiniteInfo.MassVec.rawMassLog2Weight`]); [`Rat`] / [`Nat.log2`] from the Lean prelude, not Mathlib. No `sorry` tactics.
- `ThermoPMFKLSingleton.lean`: shared singleton [`MeasurableSingletonClass`] Radon–Nikodym identities [`ThermoPMFKLSingleton.rnDeriv_mul_measure_singleton`] / [`ThermoPMFKLSingleton.rnDeriv_toReal_eq_div`] factored out for [`ThermoPMFKLFintype`] (and anything else that needs the discrete RN ratio). Proofs use Mathlib `Measure.setLIntegral_rnDeriv'` from [`Mathlib.MeasureTheory.Measure.Decomposition.RadonNikodym`]. No `sorry` tactics.
- `ThermoPMFKLFintype.lean`: general [`ThermoPMFKLFintype.klDiv_pmfFintype_toReal_eq_sum_klFun_singleton_mass`] — for finite `α`, `(klDiv p.toMeasure q.toMeasure).toReal` as a `∑_z ν({z})·klFun(μ({z})/ν({z}))` sum; nonnegative-real packaging [`ThermoPMFKLFintype.pmfOfNonnegReal`] and [`ThermoPMFKLFintype.klDiv_pmfOfNonnegReal_toReal_eq_sum_klFun_ratio`]; used by [`ThermoPMFKLBridge`] and [`ThermoUniformKLBridge`]. No `sorry` tactics.
- `ThermoFiniteKLCore.lean`: convenience import only — aggregates [`ThermoInformationBridge`] + [`ThermoPMFKLFintype`] (pulls [`ThermoPMFKLSingleton`]) for downstream files that want Shannon + finite `PMF` KL without importing the joint or uniform Mathlib bridges. No `sorry` tactics.
- `ThermoPMFKLBridge.lean`: `PMF (ι × κ)` packaging [`jointPMFOfReal`] / [`prodMarginalPMFOfReal`] from the same nonnegative `ℝ` masses as [`ThermoInformationBridge`], with `ENNReal` normalization lemmas and [`klDiv_toReal_eq_mutualInformation`] (`InformationTheory.klDiv` on `PMF.toMeasure` agrees with [`mutualInformation_eq_klFun_sum`] under [`MeasurableSingletonClass (ι × κ)`]). No `sorry` tactics.
- `ThermoUniformKLBridge.lean`: `PMF ι` from nonnegative masses ([`massPMFOfReal`]) and the discrete uniform [`uniformPMF`], with [`klDiv_uniform_toReal_eq`] identifying `klDiv.toMeasure` KL to uniform with `log |ι| − H(p)` (via [`sum_inv_card_klFun_eq_log_card_sub_shannonEntropy`]) under [`MeasurableSingletonClass ι`]. No `sorry` tactics.
- `ThermoInformationBridge.lean`: finite Shannon entropy (`shannonEntropy`, `shannonEntropyOnFinset`, `shannonEntropy_eq_shannonEntropyOnFinset`), independent coupling `prodPMF` with `mul_mul_log_mul` and `shannonEntropy_prod_independent`, marginals `marginalX` / `marginalY` with joint–log–marginal lemmas, `conditionalEntropyXGivenY` / `conditionalEntropyYGivenX`, chain rules `chain_rule_joint_add_marginalX` / `chain_rule_joint_add_marginalY`, `shannonEntropy_sub_marginalX` / `shannonEntropy_sub_marginalY`, `mutualInformation_marginals` / `mutualInformation_eq_sub_X` / `mutualInformation_eq_sub_Y`, `mutualInformation_eq_sum_joint_log_div` / `mutualInformation_eq_klFun_sum` / `mutualInformation_nonneg` / `shannonEntropy_joint_le_add_marginals` (nonnegative joint PMF + normalization), `conditionalEntropy_prodPMF` / `conditionalEntropyY_prodPMF`, `mutualInformation_prod_independent`, uniform-on-`Finset` `shannonEntropy_uniform_on_finset`, uniform Fintype `shannonEntropy_uniform_fintype`, product-uniform `shannonEntropy_prod_uniform`, fair `Fin 2` `shannon_fair_binary_fin2`, `landauer_matches_shannon_fair_bit`. No `sorry` tactics.
- `FiniteIndPrgGames.lean`: finite IND-PRG as `PMF` (`gameReal` / `gameIdeal`), `prTrue`, `advantage : ℝ`; trivial zero-advantage lemmas for constant distinguishers. Namespace `CryptographicPseudorandom.FiniteIndPrg`. No `sorry` tactics.
- `FiniteIndPrfGames.lean`: one-query PRF (`gameReal` / `gameIdeal` with `bind`), `advantage`, constant-distinguisher lemmas; imports `FiniteIndPrgGames` for `prTrue`. Namespace `CryptographicPseudorandom.FiniteIndPrf`. No `sorry` tactics.
- `LaceyNestedRotation.lean`: `rotationAt`, `helixDist` / `isAlphaHalfSpec` (`Fin 54`), `rotationAt_block`; constants 11 / 54 / 7920. Zero sorry.
- `UniversalSignalMap.lean`: Formalizes the Monster Mesh as a Universal Signal Map that translates logic (Fork/Race/Fold) and Polyglot IR (Spawn/Join/Branch) into deterministic topological pathways. Includes resonance colors, ER=EPR wormhole logic, and Stone Space sieve mapping.
- `apps/storms-watch/StormsWatchReduction.lean`: assumption-parameterized Lean
  scaffold for the `storms.watch` theorem and anti-theorem surface, including
  39 named theorem targets for reduced-state weather-object forecasting,
  source-void ingestion, trident void modeling, and hurricane specialization
  boundaries. That module now carries the concrete `ForcingPacket`,
  `StormTransitionLaw`, `StormVoidClassifier`, `StormObservationBalance`,
  `StormVoidState`, and `StormTridentForecast` structures plus the hardened
  void, neighborhood, and offset-family theorem surface.
