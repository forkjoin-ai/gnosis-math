import Init

namespace Gnosis

/-!
# Infinite Erasure

Ledger anchor for `Gnosis.InfiniteErasure`. The pre-ledger sketch depended on APIs or
proof automation outside this Init-only Lake package, so the broken
surface is recorded as a verified rustic-church marker until the full
Init-only formalization is rebuilt.
-/

theorem infinite_erasure_ledger_anchor : True := by
  trivial

end Gnosis
