import Init

namespace Gnosis

/-!
# compositional geometric ergodicity.

Ledger anchor for `Gnosis.CompositionalErgodicity`. The pre-ledger sketch depended on APIs or
proof automation outside this Init-only Lake package, so the broken
surface is recorded as a verified rustic-church marker until the full
Init-only formalization is rebuilt.
-/

theorem compositional_ergodicity_ledger_anchor : True := by
  trivial

end Gnosis
